function main() {
    other(10, 11, 12, 13, 14, 15);
}

function other(test: u32, a: u32, b: u32, c: u32, d: u32, e: u32) {
    let ret: u32 = 10 + test;
    let other_v: u32 = e;
}

function bools(a: bool, b: bool, c: bool, d: bool, e: bool, f: bool) {
    let val: bool = f;
}